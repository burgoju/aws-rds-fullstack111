
## 🌐 Live Demo
- **Frontend:** [Your S3 Website URL]
- **Backend API:** `http://your-ec2-ip:3000/api`
- **API Health:** `http://your-ec2-ip:3000/api/health`

## 🛠️ Tech Stack
### Backend
- **Runtime:** Node.js 18.x
- **Framework:** Express.js
- **Database:** AWS RDS MySQL 8.0
- **ORM:** mysql2 (native)
- **Process Manager:** PM2

### Frontend
- **Hosting:** Amazon S3 Static Website
- **Languages:** HTML5, CSS3, JavaScript (Vanilla)
- **Icons:** Font Awesome 6
- **Design:** Responsive, Mobile-First

### AWS Services
1. **Amazon RDS** - Managed MySQL database
2. **Amazon EC2** - Backend server hosting
3. **Amazon S3** - Frontend static hosting
4. **Security Groups** - Network security
5. **IAM** - Access management

## 📊 Database Schema
```sql
-- Users table
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Tasks table  
CREATE TABLE tasks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    title VARCHAR(255) NOT NULL,
    status ENUM('pending','in_progress','completed'),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- Posts table
CREATE TABLE posts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    likes INT DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
